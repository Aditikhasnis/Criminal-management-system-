def get_error_message(self, error_code):
        # # Query the age_error_messages table for the error message based on the error code
        #     self.my_cursor.execute('SELECT error_message FROM age_error_messages WHERE error_code=?', (error_code,))
        #     result = self.my_cursor.fetchone()
        #     if result:
        #      return result
        #     else:
        #      return "Unknown Error"  
          




        #   age = self.var_age.get()
        #   if age < 18 or age > 120:
        #     error_code = 2  # Indication (error code) for "Age must be between 18 and 120"
        #     error_message = self.get_error_message(error_code)
        #     ttk.messagebox.showerror("Error", error_message,PanedWindow)
        #     return
        #   if not age.isdigit():
        #     error_code = 1  # Indication (error code) for "Age must be a positive integer"
        #     error_message = self.get_error_message(error_code)
        #     ttk.messagebox.showerror("Error", error_message,PanedWindow)
        #   age = int(age) 